"""Plan 118 — opt-in real integration matrix (NOT collected by the unit-test runner).

Run with: DUCKLN_INTEGRATION=1 PYTHONPATH=src python -m tests.integration.run
It actually clones, sets up, and runs each repo on each environment, asserts it reaches
"running", and reports a pass-rate — the empirical gate for the "99%" claim.
"""
